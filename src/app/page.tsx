import { Hero } from "@/components/Hero";

// Home is the hero as the dominant piece; the header carries navigation to the
// other pages. Uses the default site metadata (see layout.tsx).
export default function Home() {
  return <Hero />;
}
